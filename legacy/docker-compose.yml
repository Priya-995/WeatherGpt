version: "3.9"

services:
  # ── Frontend ─────────────────────────────────────────────────────
  # Next.js 14 web application.
  # Build logic, port mappings, and volume mounts will be added
  # in a later phase once the app is production-ready.
  frontend:
    image: placeholder  # TODO: replace with build config
    # build:
    #   context: ./frontend
    #   dockerfile: Dockerfile
    # ports:
    #   - "3000:3000"

  # ── Backend ──────────────────────────────────────────────────────
  # Python FastAPI service.
  # Build logic, port mappings, and env_file will be added
  # in a later phase once the API is production-ready.
  backend:
    image: placeholder  # TODO: replace with build config
    # build:
    #   context: ./backend
    #   dockerfile: Dockerfile
    # ports:
    #   - "8000:8000"
    # env_file:
    #   - ./backend/.env
